# Дополнение к ТЗ: Ипотечный калькулятор — v16 (расчёт необходимого дохода / ПДН)

Новая функция: расчёт минимального ежемесячного дохода, необходимого для
получения ипотеки, исходя из ежемесячного платежа и допустимой доли дохода,
которую платёж может занимать (аналог банковского показателя ПДН —
"показатель долговой нагрузки").

---

## Входные данные (новые поля)

| Параметр | Описание | По умолчанию |
|---|---|---|
| Порог ПДН, % | Максимальная доля дохода, которую может занимать сумма всех платежей | 50 |
| Другие ежемесячные платежи, ¤ | Сумма других обязательств заёмщика (кредиты, займы и т.д.) | 0 |

Оба поля — общие для всего калькулятора, не привязаны к конкретной вкладке
(размещаются рядом с остальными общими параметрами, например, там же, где
уже есть "Знак валюты" — см. v8).

---

## Формула расчёта

```
Необходимый_доход = (Платёж_для_расчёта + Другие_платежи) / (Порог_ПДН / 100)
```

Где **Платёж_для_расчёта** зависит от типа платежа:

```
Если Тип_платежа == "Аннуитетный":
    Платёж_для_расчёта = P   // постоянный платёж

Если Тип_платежа == "Дифференцированный":
    Платёж_для_расчёта = P_1   // платёж в 1-м (самом большом) месяце
```

Обозначения:
- **Порог_ПДН** — введённый пользователем процент (по умолчанию 50)
- **Другие_платежи** — введённая пользователем сумма прочих ежемесячных
  обязательств (по умолчанию 0)
- **P** — ежемесячный платёж (аннуитет)
- **P_1** — платёж в первом месяце (дифференцированный, самый большой)

### Пример расчёта

Платёж (аннуитет) = 403 481,63 ¤, другие платежи = 15 000 ¤, порог = 50%

```
Необходимый_доход = (403 481,63 + 15 000) / 0,5 = 836 963,26 ¤
```

---

## Где отображается расчёт

Показатель "Необходимый ежемесячный доход" отображается **на всех
вкладках**, где уже показывается ежемесячный платёж:

1. **"Без доп. платежей"** — рядом с блоком "Итоги" (ежемесячный платёж,
   итого выплат, переплата, тело кредита)
2. **"Досрочное погашение с уменьшением срока кредита"** — рядом с
   "Ваш текущий платёж"
3. **"Досрочное погашение с уменьшением платежа"** — рядом с
   "Ваш текущий платёж"

На всех вкладках расчёт ведётся от **базового** платежа (P или P_1) —
**без учёта** доп. платежа по досрочному погашению. Логика: доп. платёж —
это добровольная переплата сверх обязательного минимума, банк при оценке
платёжеспособности ориентируется на официальный ежемесячный платёж по
графику, а не на желание заёмщика гасить больше.

### Как оформить в UI

Пример блока (аналогично уже реализованным крупным акцентным блокам,
см. v4, п.3):

```
Ежемесячный платёж              Необходимый доход
   403 481,63 ¤                    836 963,26 ¤
                                (при ПДН ≤ 50% и др. платежах 15 000 ¤)
```

- Можно разместить оба показателя рядом (платёж / необходимый доход) —
  чтобы сразу было видно причинно-следственную связь
- Подпись снизу мелким шрифтом — с указанием использованных Порог_ПДН
  и Других_платежей, чтобы пользователь не терял контекст, по каким
  параметрам посчитан доход

---

## Валидация новых полей

- Порог ПДН: положительное число, разумные границы (например, от 1 до 100%)
- Другие ежемесячные платежи: не может быть отрицательным числом

## Технические детали

- Оба новых поля (Порог ПДН, Другие платежи) должны поддерживать
  live-форматирование чисел (см. v4, п.1) для поля "Другие платежи" —
  это сумма, форматируется так же, как остальные суммы
- Порог ПДН вводится как процент — с той же точностью до 0,1, что и
  процентная ставка (см. v3, п.3), чтобы можно было ввести, например, 45.5%
- Расчёт "Необходимый доход" пересчитывается **по той же кнопке
  "Посчитать"**, что и остальные показатели (см. v4, п.2) — не в реальном
  времени
